version: 3

repositories:
  - _comment: .vimrc (Linux x64 profile)
    url: https://raw.githubusercontent.com/pirakansa/bootkit/main/assets/
    files:
      - file_name: vimrc
        rename: .vimrc
        out_dir: $HOME
