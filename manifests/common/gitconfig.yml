version: 1

repositories:
  - _comment: .gitconfig
    url: https://raw.githubusercontent.com/pirakansa/bootkit/main/assets/
    files:
      - file_name: gitconfig
        rename: .gitconfig
        out_dir: $HOME
